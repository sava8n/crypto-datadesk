"""Reusable building blocks shared across the analytics features.

- ``black76``      — vectorized Black-76 option-pricing primitives.
- ``quotes``       — Deribit book-summary parsing/filtering (``prepare_quotes``).
- ``market_data``  — request helpers (currency validation, upstream loading).
"""
